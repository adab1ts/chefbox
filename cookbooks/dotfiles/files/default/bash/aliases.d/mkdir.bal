# File: $ADMIN_PATH/dotfiles/bash/aliases.d/mkdir.bal


alias md='mkdir -v'
alias mdm='mkdir -vm'        # mdm mode (e.g. mdm 750)
alias mdp='mkdir -vp'
alias md+='sudo mkdir -v'
alias mdm+='sudo mkdir -vm'  # mdm mode (e.g. mdm 750)
alias mdp+='sudo mkdir -vp'

